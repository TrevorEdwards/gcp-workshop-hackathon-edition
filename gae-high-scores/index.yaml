# This doesn't work yet because gcloud doesn't support YAML anchors

# id: string
# year: string (one of { freshman, sophomore, junior, senior, graduate, professor })
# [...other fields aren't read by the scoreboard client]
PARTICIPANT_KIND: &PARTICIPANT_KIND HighScoreEntry8
# id: string
# problemId: string
# score: number
# timestamp: number
ATTEMPT_KIND: &ATTEMPT_KIND Attempt8

# gcloud datastore create-indexes index.yaml
indexes:
- kind: *ATTEMPT_KIND
  properties:
  - name: score
    direction: desc
  - name: timestamp
